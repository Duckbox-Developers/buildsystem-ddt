#!/bin/bash

mkdir -p BUILDLOG

if [ ! -z $1 ] ; then
#	time make $1 2>&1 | tee BUILDLOG/make_$(date +%d.%m.%Y_%H%M).log
	time make $1 2>&1 | tee BUILDLOG/make.log
else
	echo "Usage: makelog some_commands"
fi
